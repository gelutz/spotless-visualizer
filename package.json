{
  "name": "spotless-visualizer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Toggle a Spotless step, see what it does.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
